#include <stdio.h>
#include <string.h>
/**
 * rev_string - Reverse string provided
 * @s: Input pointe
 */
void rev_string(char *s)
{
	int lengtj = strlen(s);
	int i;

	for (i = length - 1; i >= 0; i--)
	{
		putchar(s[i]);
	}
	putchar('\n);
}
